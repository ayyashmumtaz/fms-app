# Copy to .env and adjust
PORT=8080

# PostgreSQL DSN (recommended)
DATABASE_URL=postgres://fms_user:fms_password@localhost:5432/fms_db?sslmode=disable

# Optional: set to "true" to auto-seed sample rows on startup (dev only)
SEED_SAMPLE=false
